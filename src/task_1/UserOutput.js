import React, {useState} from 'react';

const UserOutput = (props) => {

    return (
        <div>
            <p>
                {props.name}
            </p>
        </div>
    )
}

export default UserOutput
